
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	// СтандартныеПодсистемы.ПодключаемыеКоманды
	ПодключаемыеКоманды.ПриСозданииНаСервере(ЭтотОбъект);
	// Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
	//Добавление реквизита "СогласованнаяСкидка" на форму документа 
	
	ЭлементСкидка = Элементы.Добавить("Наг_СогласованнаяСкидка", Тип("ПолеФормы"),Элементы.ГруппаШапкаЛево);
	ЭлементСкидка.Заголовок = "Согласованная скидка";
	ЭлементСкидка.Вид = ВидПоляФормы.ПолеВвода;
	ЭлементСкидка.ПутьКДанным = "Объект.Наг_СогласованнаяСкидка";
	ЭлементСкидка.УстановитьДействие("ПриИзменении", "СкидкаПриИзменении");
	//-- 
	
	
	//Добавление кнопки "Перерасчитать сумму с учетом скидки"
	
	Команда = Команды.Добавить("ПерерасчитатьСУчетомСкидки");
	Команда.Заголовок = "Перерасчитать с учетом скидки";
	Команда.Действие = "Наг_КомандаПерерасчитатьСУчетомСкидки";
	
	КнопкаФормы = Элементы.Добавить("КнопкаПерерасчитатьСУчетомСкидки", Тип("КнопкаФормы"),ЭтотОбъект.КоманднаяПанель);
	КнопкаФормы.ИмяКоманды = "ПерерасчитатьСУчетомСкидки";
	КнопкаФормы.Вид = ВидКнопкиФормы.ОбычнаяКнопка;	  
	
	//-- 
	
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
    // СтандартныеПодсистемы.ПодключаемыеКоманды
    ПодключаемыеКомандыКлиент.НачатьОбновлениеКоманд(ЭтотОбъект);
    // Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
КонецПроцедуры

&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
    // СтандартныеПодсистемы.ПодключаемыеКоманды
    ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Объект);
    // Конец СтандартныеПодсистемы.ПодключаемыеКоманды
	
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
    ПодключаемыеКомандыКлиент.ПослеЗаписи(ЭтотОбъект, Объект, ПараметрыЗаписи);
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыТовары

&НаКлиенте
Процедура ТоварыКоличествоПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыЦенаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура ТоварыПриИзменении(Элемент)
	РассчитатьСуммуДокумента();
КонецПроцедуры

&НаКлиенте
Процедура ТоварыСкидкаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Товары.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыУслуги

&НаКлиенте
Процедура УслугиКоличествоПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура УслугиЦенаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура УслугиСкидкаПриИзменении(Элемент)
	
	ТекущиеДанные = Элементы.Услуги.ТекущиеДанные;
	
	РассчитатьСуммуСтроки(ТекущиеДанные);
	
КонецПроцедуры

&НаКлиенте
Процедура УслугиПриИзменении(Элемент)
	РассчитатьСуммуДокумента();
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаКлиенте
Процедура РассчитатьСуммуСтроки(ТекущиеДанные)
	
	// ++ Нагайцева О.А. 22.01.2024
	//Переопределение процедуры расчета суммы
	
	//Исходный код: 
	//ТекущиеДанные.Сумма = ТекущиеДанные.Цена * ТекущиеДанные.Количество;
	//
	//НовыйКод
    	
	ЦенаСоСкидкой(ТекущиеДанные);
	
	//--Нагайцева О.А.  
	
	РассчитатьСуммуДокумента();   
КонецПроцедуры

&НаКлиенте
Процедура РассчитатьСуммуДокумента()
	
	Объект.СуммаДокумента = Объект.Товары.Итог("Сумма") + Объект.Услуги.Итог("Сумма");
	
КонецПроцедуры

#Область ПодключаемыеКоманды

// СтандартныеПодсистемы.ПодключаемыеКоманды
&НаКлиенте
Процедура Подключаемый_ВыполнитьКоманду(Команда)
    ПодключаемыеКомандыКлиент.НачатьВыполнениеКоманды(ЭтотОбъект, Команда, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ПродолжитьВыполнениеКомандыНаСервере(ПараметрыВыполнения, ДополнительныеПараметры) Экспорт
    ВыполнитьКомандуНаСервере(ПараметрыВыполнения);
КонецПроцедуры

&НаСервере
Процедура ВыполнитьКомандуНаСервере(ПараметрыВыполнения)
    ПодключаемыеКоманды.ВыполнитьКоманду(ЭтотОбъект, ПараметрыВыполнения, Объект);
КонецПроцедуры

&НаКлиенте
Процедура Подключаемый_ОбновитьКоманды()
    ПодключаемыеКомандыКлиентСервер.ОбновитьКоманды(ЭтотОбъект, Объект);
КонецПроцедуры
// Конец СтандартныеПодсистемы.ПодключаемыеКоманды

#КонецОбласти

#КонецОбласти

#Область ДоработкиНагайцевой

&НаКлиенте  
Асинх Процедура СкидкаПриИзменении(Элемент)
	//++ Нагайцева 22.01.24 
	//Перерасчитать скидку
	СтрокаТовары = Элементы.Товары.ТекущаяСтрока;
	СтрокаУслуги = Элементы.Услуги.ТекущаяСтрока;
	Если СтрокаТовары = Неопределено И СтрокаУслуги = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Обещание = ВопросАсинх("Перерасчитать табличную часть, учитывая скидку?", РежимДиалогаВопрос.ДаНет);
	Результат = Ждать Обещание; 	
	Если Результат = КодВозвратаДиалога.Да Тогда
		Наг_РассчитатьСоСкидкой();
	КонецЕсли;
	
	//-- Нагайцева
КонецПроцедуры

&НаКлиенте
Процедура Наг_РассчитатьСоСкидкой(); 
	//++ Нагайцева 22.01.24 
	//Перерасчитать скидку    
	
	ТаблицаТовары = Объект.Товары;
	ТаблицаУслуги = Объект.Услуги;
	
	Для Каждого Строка Из ТаблицаТовары Цикл      
		ЦенаСоСкидкой(Строка);
	КонецЦикла;
	
	Для Каждого Строка Из ТаблицаУслуги Цикл      
		ЦенаСоСкидкой(Строка);
	КонецЦикла;
	
	//-- Нагайцева
	   
КонецПроцедуры 

&НаКлиенте
Процедура ЦенаСоСкидкой(Строка)
	//++ Нагайцева 22.01.24 
	//Перерасчитать цены со скидкой в таблицах  
	
	ЦенаБезСкидки = (Строка.Цена * Строка.Количество);
	
	ЦенаСоСкидкой = ЦенаБезСкидки - ЦенаБезСкидки *(Объект.Наг_СогласованнаяСкидка/100);
	
	Строка.Сумма = ЦенаСоСкидкой;
	
	//-- Нагайцева
	
КонецПроцедуры 	

&НаКлиенте
Процедура  Наг_КомандаПерерасчитатьСУчетомСкидки(Команда);      
 	//++ Нагайцева 29.01.24 
	//Действие команды Перерасчитать цены со скидкой в таблицах  

 Наг_РассчитатьСоСкидкой()
 
 //-- Нагайцева
 
КонецПроцедуры
#КонецОбласти 

